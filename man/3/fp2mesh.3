FP2MESH(3)            ESTIVA ライブラリ関数マニュアル                FP2MESH(3)

名称
     fp2mesh - ストリームからmesh形式のデータを格納

ライブラリ
     ESTIVA ライブラリ (libestiva, -lestiva)

書式
     #include <estiva/mesh.h>

     void
     fp2msh(FILE* fp, xyc** Zp, nde** Np);

解説
     fp2mesh()ルーチンはストリームらmesh形式のデータを格納します。
     ストリームのデータが、接点-座標対応表の場合は、
     デローニー三角分割を行い、Zpに接点-座標対応表を Npに要素-接点
     対応表を格納します。

     ストリームのデータが、mesh形式のデータの場合は、そのまま
     Zp, Np にデータを格納します。

戻り値
     戻り値はありません。

関連項目
     xyc2msh(1), xmesh(1)
